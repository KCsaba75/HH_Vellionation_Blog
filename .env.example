# ============================================================================
# VELLIO NATION - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to `.env` and fill in your actual Supabase credentials
# DO NOT commit .env to git - it's already in .gitignore
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (REQUIRED)
# ----------------------------------------------------------------------------

# Your Supabase Project URL
# Get this from: Supabase Dashboard → Settings → API → Project URL
# Example: https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_URL=your_supabase_project_url_here

# Your Supabase Anonymous (Public) Key
# Get this from: Supabase Dashboard → Settings → API → Project API keys → anon public
# This key is safe to use in frontend code
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# ----------------------------------------------------------------------------
# If you're setting up Supabase for the first time or recreating your database:
# 1. See SETUP_GUIDE.md for complete step-by-step instructions
# 2. Create a new Supabase project at https://supabase.com/dashboard
# 3. Run the SQL migrations in supabase_migrations/ folder (in order!)
# 4. Create 4 storage buckets: avatars, post_images, solution_images, community_post_images (all PUBLIC!)
# 5. Copy your credentials from Supabase Dashboard → Settings → API
# 6. Paste them above, replacing the placeholder values
# 7. Save this file as `.env` (without the .example extension)
# 8. Restart the dev server
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# SECURITY NOTES
# ----------------------------------------------------------------------------
# ⚠️ NEVER use the service_role key in frontend code - it bypasses all security!
# ✅ The anon public key is safe to use - it respects Row Level Security (RLS) policies
# ✅ The .env file is in .gitignore and won't be committed to version control
# ----------------------------------------------------------------------------
